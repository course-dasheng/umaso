// import React from 'react'
// import type { ReactTestRendererJSON } from 'react-test-renderer'
// import renderer from 'react-test-renderer'
import { describe, expect, it } from 'vitest'
// import { ElHeader, ElAside, ElMain, ElFooter, ElContainer } from './index'

describe('ElHeader', () => {
  it('ElHeader render', () => {
    // const component = renderer.create(
    //   <ElHeader></ElHeader>,
    // )
    // const { type } = component.toJSON() as ReactTestRendererJSON
    // expect(type).toBe('el-header')
    expect(1).toBe(1)
  })
})
